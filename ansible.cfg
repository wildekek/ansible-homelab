[defaults]
inventory = ./inventory
private-key-file = ~/.ssh/id_ansible
remote_user = willem
interpreter_python = auto_silent
# For local development: reads from .vault_pass
# For CI/CD: can be overridden via ANSIBLE_VAULT_PASSWORD environment variable
vault_password_file = .vault_pass
