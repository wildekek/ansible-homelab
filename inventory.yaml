---
# Machine groups
physical_machines:
  hosts:
    office-pi:

qnap_containers:
  hosts:
    pbs:
      ansible_user: root

# Role groups
role_pbs_client:
  hosts:
    office-pi:

role_labelprinter:
  hosts:
    office-pi:

# VM-specific overrides
vm_overrides:
  hosts:
    proxmox1:
      ansible_user: root
